stage('Publish to JFrog') {
    steps {
        script {
            artifact.pushToJFrog(
                username: env.ARTIFACTORY_USER,
                password: env.ARTIFACTORY_PASSWORD,
                repository: 'libs-release-local',
                artifactPath: 'target/my-app.jar'
            )
        }
    }
}
